import type{BaseEntity}from'./foundation';
export interface SubcontractRecord extends BaseEntity{number:string;projectId:string;subcontractorId:string;title:string;originalValue:number;variations:number;revisedValue:number;retentionRate:number;advanceAmount:number;startDate:string;endDate?:string;status:'draft'|'active'|'closed'|'cancelled'}
export interface ClientIPC extends BaseEntity{number:string;projectId:string;contractId:string;clientId:string;date:string;previousCertified:number;currentWork:number;materialsOnSite:number;grossAmount:number;retentionAmount:number;advanceRecovery:number;vatAmount:number;deductions:number;netDue:number;status:'draft'|'posted'|'cancelled';clientInvoiceId?:string}
export interface ClientIPCLine extends BaseEntity{ipcId:string;boqItemId:string;previousQuantity:number;currentQuantity:number;cumulativeQuantity:number;rate:number;amount:number}
export interface MaterialsOnSiteRecord extends BaseEntity{number:string;projectId:string;contractId:string;boqItemId:string;description:string;quantity:number;rate:number;amount:number;eligibleForIPC:boolean;status:'draft'|'approved'|'included'|'cancelled';ipcId?:string}
export interface HandoverRecord extends BaseEntity{number:string;projectId:string;kind:'partial'|'final';date:string;scope:string;openItems:string;acceptedByClient:boolean;status:'draft'|'submitted'|'accepted'|'cancelled'}
export interface RetentionReleaseRecord extends BaseEntity{number:string;partyType:'client'|'supplier'|'subcontractor';partyId:string;projectId:string;sourceType:'contract'|'subcontract';sourceId:string;date:string;amount:number;status:'draft'|'approved'|'paid'|'cancelled'}
