import type { BaseEntity } from './foundation';
export interface SupplyLotRecord extends BaseEntity{number:string;projectId:string;contractId:string;boqItemId:string;plannedQuantity:number;plannedDeliveryDate:string;deliveryLocationType:'main_warehouse'|'project_warehouse'|'client_site';deliveryLocationId?:string;status:'draft'|'approved'|'closed'|'cancelled'}
export interface ServiceOrderRecord extends BaseEntity{number:string;projectId:string;supplierId:string;description:string;quantity:number;unit:string;unitPrice:number;totalValue:number;requiredBy:string;status:'draft'|'approved'|'completed'|'cancelled'}
export interface CommercialNote extends BaseEntity{number:string;partyType:'client'|'supplier';partyId:string;sourceType:'client_invoice'|'supplier_invoice';sourceId:string;kind:'credit'|'debit';date:string;amount:number;reason:string;status:'draft'|'posted'|'cancelled';journalId?:string}
export interface SupplyQuantityRow{boqItemId:string;contracted:number;required:number;ordered:number;purchased:number;inTransit:number;received:number;delivered:number;accepted:number;rejected:number;invoiced:number;returned:number;remaining:number}
export interface ThreeWayMatchRow{purchaseOrderId:string;poNumber:string;orderedQuantity:number;receivedQuantity:number;orderedValue:number;invoicedValue:number;quantityVariance:number;valueVariance:number;status:'matched'|'quantity_variance'|'value_variance'|'both_variance'}
